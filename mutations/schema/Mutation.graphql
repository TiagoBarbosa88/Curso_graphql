type Mutation {
	# novoUsuario(
	#   nome: String,
	#   email: String,
	#   idade: Int
	# ): Usuario!

	# Adicionando Input
	novoUsuario(
    dados: UsuarioInput!
    ): Usuario!

	# excluirUsuario(
	#   id: Int
	# ): Usuario

	# Adicionando Input Excluir
	excluirUsuario(
    filtro: UsuarioFiltro!
    ): Usuario

	# alterarUsuario(
	#   id: Int,
	#   nome: String,
	#   email: String,
	#   idade: Int
	# ): Usuario

 # Adicionando Input Alterar
	alterarUsuario(
    filtro: UsuarioFiltro!, 
    dados: UsuarioInput!
    ): Usuario
}
