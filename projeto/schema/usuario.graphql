type Perfil {
	id: Int
	nome: String!
}

enum UsuarioStatus {
	ATIVO
	INATIVO
	BLOQUEADO
}

type Usuario {
	id: Int
	nome: String!
	idade: Int
	email: String!
	salario: Float
	vip: Boolean
	perfil: Perfil
	status: UsuarioStatus
}